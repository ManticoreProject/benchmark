# Makefile for plus-prefix benchmark
#
# @configure_input@
#

SHELL =		/bin/sh

SMLNJ =		@SMLNJ_CMD@
MLTON =		@MLTON@
PMLC =		@PMLC@
PMLCFLAGS =	@PMLCFLAGS@

TARGET =	plus-prefix

all:		$(TARGET) $(TARGET)-smlnj

$(TARGET):	plus-prefix.mlb plus-prefix.pml
	$(PMLC) $(PMLCFLAGS) -Csequential=true -o $(TARGET) plus-prefix.mlb

$(TARGET)-smlnj:	plus-prefix.cm plus-prefix.sml
	echo "SMLofNJ.exportFn(\"plus-prefix\", Main.main);" | $(SMLNJ) plus-prefix.cm

.PHONY: clean
clean:
	rm -f $(TARGET) *.s
